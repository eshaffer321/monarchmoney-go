query GetCashflowSimple($startDate: Date!, $endDate: Date!) {
  aggregates(
    filters: {
      startDate: $startDate
      endDate: $endDate
    }
  ) {
    summary {
      sumIncome
      sumExpense
      savings
      savingsRate
    }
  }
}