mutation SetBudgetAmount($input: SetBudgetAmountMutationInput!) {
  setBudgetAmount(input: $input) {
    budget {
      id
      amount
      rollover
    }
    errors {
      message
      code
    }
  }
}