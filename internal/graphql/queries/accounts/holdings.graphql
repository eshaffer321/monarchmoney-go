query GetAccountHoldings($accountId: ID!) {
  account(id: $accountId) {
    id
    holdings {
      edges {
        node {
          id
          symbol
          quantity
          price
          value
          costBasis
          updatedAt
          holding {
            name
          }
        }
      }
    }
  }
}
