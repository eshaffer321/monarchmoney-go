query CheckRefreshStatus($accountIds: [UUID!]) {
  accounts(accountIds: $accountIds) {
    id
    credential {
      updateRequired
      disconnectedFromDataProviderAt
    }
    syncing
    lastSyncedAt
  }
}